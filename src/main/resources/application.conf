akka {
  loglevel = DEBUG
}

http {
  interface = "0.0.0.0"
  port = 9000
}

services {
  products-api {
    host = "products-api.com"
    port = 80
  }
}

cassandra {
  host     : ["localhost"]
  keyspace : "Products"
  username : "master"
  password : "master"
}